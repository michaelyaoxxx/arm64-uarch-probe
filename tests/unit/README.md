# Unit Tests

Isolated tests for domain models, strict registry validation, configured
platform resolution, planning, and serialization. Use real immutable records
and keep host behavior out of unit tests.
